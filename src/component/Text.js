import styled from 'styled-components';
import { DEFAULT_FONT, COLOR_WHITE } from '../styles';

const Text = styled.p`
    ${DEFAULT_FONT}
    color: ${COLOR_WHITE};
    text-align: center;
    font-size: 2em;
`;

export default Text;
